services:
  ui:
    build: .
    profiles: ["homolog", "main"]
    ports:
      - "25565:25565"
    environment:
      NUXT_PUBLIC_API_BASE: ${NUXT_PUBLIC_API_BASE:-http://localhost/api}
      PORT: ${PORT:-25565}
    networks:
      - shared-network

networks:
  shared-network:
    external: true
